New Student
<%= form_for(@student) do |f| %> 
	<%= f.label :declared_major %> 
	<%= f.text_field :declared_major %>
	<br/>
	<%= f.label :credits %>
	<%= f.text_field :credits %>
	<br/>
	<%= f.submit "Create Student" %> 
<% end %> 